# Copy this file to .env.local and fill in your values.
# The Next.js rewrite proxies /api/* to this URL, so the browser
# never needs to know the FastAPI address directly.
NEXT_PUBLIC_API_URL=http://localhost:8000
