apiVersion: v1
kind: ConfigMap
metadata:
  name: ci-agent-config
  namespace: ci-agent
  labels:
    app.kubernetes.io/part-of: ci-agent
data:
  CI_AGENT_MODEL: "claude-sonnet-4-20250514"
  CORS_ORIGINS: "http://localhost:3000"           # Update to your frontend domain
